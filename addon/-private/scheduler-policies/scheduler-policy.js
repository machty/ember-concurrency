import { CANCELLED, STARTED } from "./desired-states";

class SchedulerPolicy {
  refresh(taskInstances) {
  }

  startTaskInstance(taskInstance) {
    // taskInstance._start();
    // set(this, 'lastStarted', taskInstance);

  }
}
