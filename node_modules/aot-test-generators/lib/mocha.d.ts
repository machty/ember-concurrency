import TestGenerator from './test-generator';
export default class MochaTestGenerator implements TestGenerator {
    suiteHeader(suiteName: string): string;
    suiteFooter(): string;
    test(testName: string, passed: boolean, assertionMessage?: string): string;
    assertion(passed: boolean, assertionMessage: string): string;
}
